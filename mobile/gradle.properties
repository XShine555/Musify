org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.java.home=C:/Program Files/Java/jdk-21

kotlin.code.style=official

android.useAndroidX=true
android.nonTransitiveRClass=true

# Musify dev backend, reachable from this Windows host (see deploy/.env). The
# API and Streaming Gateway run via `dotnet run`; the rest of the stack (incl.
# Zitadel) runs in Docker with host.docker.internal resolved to the host.
musify.apiBaseUrl=http://localhost:5111
musify.authIssuer=http://host.docker.internal:8080
musify.authClientId=390116136177434631
